

## H5 报告页面修复（2026-04-11）
- H5ReportViewer 修复：prefixCSSSelectors 函数结尾缺少 } 导致 SWC 解析报错
- CSS 隔离方案：所有章节样式加 .hri-chapter-{key} 前缀，body/html 等全局选择器加 all:initial 覆盖
- 新增 Demo 模式：URL 加 ?demo=true 全部章节解锁，供演示使用
- 修复 page.tsx：HRI-2026 fallback 数据 + searchParams.demo 参数传递
