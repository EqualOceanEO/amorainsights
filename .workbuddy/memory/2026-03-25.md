# 2026-03-25 工作记录

## News 每日自动化更新

### 新增 API
- `src/app/api/admin/news-generator/route.ts` — 每日新闻生成 API
  - 每天生成 6 条一级行业新闻（每个 L1 行业 1 条）
  - 每天轮换生成 8 条公司新闻（遍历 359 家追踪公司，不重复）
  - 所有新闻带 `content` 字段（含完整正文内容）
  - 鉴权：`?secret=run-migration-now` 或 Bearer token
  - 调用：`GET /api/admin/news-generator?secret=run-migration-now`

### Vercel Cron 调度
- vercel.json 新增：`0 6 * * *` 每天 06:00 UTC 自动触发
- 现有 cron：合规扫描每周五 23:59

### 新闻详情页改造
- `src/app/news/[slug]/page.tsx`：
  - 布局改为 `xl:grid-cols-[1fr_280px]`，主内容区更宽
  - `content` 字段展示升级：`text-lg leading-loose`，阅读体验更好
  - 增加字数统计 footer（word count）
  - 侧边栏显示 sub-sector 名称（用 SUB_SECTOR_NAMES 查对应表）

### 新闻列表页
- `src/app/news/page.tsx`：添加 SUB_SECTOR_NAMES 映射，显示子行业名称而非整数 ID

### 数据修复
- `src/app/api/news/route.ts` 和 `[slug]/route.ts`：tags 字段规范化，处理 PostgreSQL array 经 REST API 返回时空格分隔字符串的情况

### 本地开发
- `.env.local` 新增 `MIGRATION_SECRET=run-migration-now`
- `scripts/check-news-status.mjs`：检查新闻数据状态
- `scripts/check-industries.mjs`：查看 industries 表 ID 映射

### 已知数据情况（2026-03-25）
- news_items 总数：68 条（持续增长）
- 359 家追踪公司（is_tracked=true）
- industries 表：L1 行业 6 个（ID 1-6），L2 子行业 36 个（ID 49-84）
