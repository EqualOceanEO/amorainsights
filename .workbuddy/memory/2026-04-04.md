# 2026-04-04 工作日志

## 首页重构完成（Homepage Aggregation）

**任务**：重构首页，聚合展示 news、companies、reports 三大模块

## HRI-2026 PPT v2 完成

**输出文件**：`HRI-2026-Report-Presentation-v2.pptx`（0.97 MB，22页）

**脚本**：`scripts/generate_hri2026_ppt_v2.cjs`（CommonJS版本，复制自 .js 源文件）

**新增内容**：
- Slide 6：技术演进时间轴（中美节点对比 2020-2028）
- Slide 7：**人形机器人产业链图谱**（基于 hri-report-part-m-mapping-v3.html，6层供应链+中美能力对比象限）
- Slide 10：客户结构与 ROI（宇树招股书数据：73.6%科研教育）
- Slide 16：AMORA Score 雷达图
- Slide 18：风险因素矩阵（6类风险）
- Slide 19：关键催化剂时间轴（2026 H1）
- Slide 20：中美深度对比

**注意**：package.json 含 `"type": "module"`，脚本必须用 `.cjs` 后缀才能运行

**实现内容（`src/app/page.tsx`）**：
- **Hero 区**：标题简化，右侧新增三个统计 Pill（Reports 数量 / Companies 数量 / News 数量），三个 CTA 按钮（Reports / Company Tracker / News）
- **Section 1 — Latest News**：
  - 左栏：Featured 大卡（横向蓝条） + 4 宫格二级新闻卡
  - 右侧边栏：3 条迷你新闻列表 + View all 链接
  - 数据来源：`news_items` 取最新 8 条
- **Section 2 — Research Reports**：
  - 顶部 Featured 大卡横跨两列 + 下方小卡网格
  - 数据来源：`reports` 表 `production_status in (published, approved)` 取最新 6 条
- **Section 3 — Top Tracked Companies**：
  - 3 列公司卡片，按 `amora_total` 降序排列，取 top 6
  - 卡片展示：名称/中文名/AMORA 评分/行业/描述/地区/融资阶段
- **Section 4 — Six Industries**：沿用原版 6 宫格
- **技术亮点**：
  - 所有数据并行 fetch（`Promise.allSettled`），任何一项失败不影响其他区块渲染
  - 直接用 supabase-js 查询（不依赖 db.ts 的 getReports 等老函数），精确 select 字段减少流量
  - 无 lint 错误

**commit**：0b05d78, df6f18e, 74b1a7a（分三次推送）

## Vercel 构建错误修复（2026-04-04）

**根因**：Vercel 一直构建失败，导致首页虽然 push 了但线上不更新

**修复内容**：
1. `package.json`：`"type": "commonjs"` → `"type": "module"`（项目全部用 ESM import/export，与 commonjs 冲突导致 78 个 Turbopack 错误）
2. `package.json`：删除 `"main": "index.js"`（Next.js 项目不需要）
3. `src/lib/db.ts`：`Company` 接口补全 30+ 缺失字段（funding_stage, valuation_usd, amora_total, ceo_name, core_products 等）
4. `src/app/page.tsx`：`latestNews` 和 `latestReports` 类型从强类型改为 `any[]`（select 子集字段无法匹配完整接口）

**本地验证**：`npx next build` 编译成功，38 个页面全部生成，零错误

**部署状态**：commit `74b1a7a` push 后 Vercel 成功构建部署，新版首页已上线（包含 Latest News / Research Reports / Top Tracked Companies）

## 首页继续精简 + Industries 导航系统（2026-04-04 下午）

**首页精简**：
- 删除底部 Newsletter section，SubscribeBox 移至 Hero 右上角玻璃卡片内（compact 模式 + stat pills）
- 删除 Top Tracked Companies 和 Six Industries sections
- Industry Pills 从 `/news?industry=` 改为 `/industries/[slug]`

**Industries 导航系统（全新）**：
- **SiteNav 改版**：Industries 按钮放在导航栏最前面，hover 展开双栏下拉菜单
  - 左栏：6 个 L1 行业（icon + name + name_cn）
  - 右栏：L2 子行业联动列表（hover 左栏时自动切换）
  - 底部 footer："6 frontier industries" + View all 链接
- **`/industries`** 页面重写：从硬编码假数据改为使用 SiteNav/SiteFooter + INDUSTRY_META 真实数据，6 宫格卡片展示
- **`/industries/[slug]` 动态页面**：L1 行业聚合页
  - Hero：行业 icon + name + name_cn + 统计数字 + 子行业 pill 标签
  - 三栏聚合：Latest News（3列卡片）/ Companies（3列卡片，含 AMORA score）/ Reports（2列卡片）
  - SSR 数据查询：`Promise.allSettled` 并行 fetch news/companies/reports
  - `generateStaticParams` + ISR
- **`/industries/[slug]/[sub]` 动态页面**：L2 子行业页，同模板 + 面包屑导航
  - 数据过滤：news/reports 用 `tags @> [sub]`，companies 用 `sub_sector = sub`

**commit**: 490acc2, de28da3, a6ae5f9

## Industries L2 路由修复（2026-04-04 傍晚）

**问题1**：去掉了所有页面的中文名（name_cn），只保留英文名
- SiteNav 下拉菜单、L1 行业页 hero、L2 子行业页 hero、industries 索引页

**问题2**：L2 子行业页面全部 404
- 根因：`INDUSTRY_HIERARCHY` 的 `level2` 从 string[] 改为 `{name, slug}[]`，用 slug（如 `cell-therapy`）作为 URL 路径，避免空格导致 Vercel CDN 无法匹配
- 修复范围：`industries.ts`、`SiteNav.tsx`、`IndustryFilterBar.tsx`、`industries/page.tsx`、`[slug]/page.tsx`、`[slug]/[sub]/page.tsx`、`news/page.tsx`、`companies/page.tsx`、`reports/page.tsx`、`news-generator/route.ts`（12 个文件）
- 新增 helper：`getLevel2Slugs()`、`getSubSectorName()`
- URL 格式：`/industries/life-sciences/cell-therapy`（之前是 `/industries/life-sciences/Cell%20Therapy`）

**commit**: eb0f8c5, acd297e, f7f31c5
