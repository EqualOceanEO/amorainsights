feat: subscribers page - add User column (name + subscription_tier from users table)

- Subscribers list API now batch-fetches users by email and returns user info per subscriber
- Subscribers admin page shows new "User" column between Email and Source
- User column displays name (or "—" if null) and color-coded tier badge (free/pro/enterprise)
- "No account" shown for subscribers without a matching users record