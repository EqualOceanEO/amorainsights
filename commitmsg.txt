fix: reports page data & companies empty state width

- reports table has no is_published column; use production_status filter instead
- set 12 existing reports production_status from draft -> published (DB update)
- companies empty state: add w-full to ensure max-w-7xl width is maintained
