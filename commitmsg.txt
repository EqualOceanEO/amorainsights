feat: complete news_items schema migration and API refactor

- Add 11 new columns to news_items: slug, content, cover_image_url, author,
  tags, is_premium, is_published, company_id, industry_id, sub_sector_id, updated_at
- Refactor /api/news to use new schema with fallback for legacy data
- Refactor /api/news/[slug] to query by slug or id
- Add /api/admin/migrate endpoint for DDL via run_migration RPC
- Add /api/admin/news CRUD endpoints with service_role key
- Update news page to use new industry_id and slug fields
- Update db.ts with full NewsItem type matching new schema
