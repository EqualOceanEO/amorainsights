fix: 宽度统一 + API 查询优化

- Companies 页面 padding 从 px-5 改为 px-6，与 Reports 页面完全一致
- 优化 /api/news 和 /api/companies 查询：
  * 显式指定 select 字段（避免 SELECT *）
  * 添加查询时间日志（Vercel console 查看）
  * 添加 try-catch 错误保护
  * pageSize 最大限制 100（避免过大数据集）
