feat(admin): redesign subscribers management — edit, delete, export, auth

- Add auth to GET /api/admin/subscribers/list (secret OR admin session)
- Add GET/PATCH/DELETE /api/admin/subscribers/[id]
- Add GET /api/admin/subscribers/export (CSV download)
- Expand list API fields: confirmed_at, plan, plan_status, stripe_customer_id
- Redesign admin subscribers page: edit modal, delete confirm, CSV export button,
  stats tabs (overview + by-source), plan badges, hover action buttons
