feat(ui): unified IndustryFilterBar component across news/companies/reports

- Create shared IndustryFilterBar component with search + two-level industry filter
- Level-2 row always in DOM (min-h-[32px]) — eliminates layout jump on level-1 click
- Supports both client-side callbacks (news/companies) and URL-mode (reports)
- Extra slot for page-specific filters (Premium toggle, Country, Listed status)
- Remove ~100 lines of duplicated filter markup from each page