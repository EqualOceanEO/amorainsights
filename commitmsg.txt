fix: news industry label, reports search, companies empty state

- News: getIndustryLabel & industryKey prefer industry_slug over industry_id
- News: fix 21 legacy records industry_id from numeric to slug in DB
- Reports: add search box (w-52, title ilike filter)
- Reports: fix is_published -> production_status filter; publish 12 records
- Companies: add w-full to empty state div to maintain max-w-7xl width
