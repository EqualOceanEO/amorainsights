fix: add stripe verify API, auto-activate Pro after checkout

- Add /api/stripe/verify endpoint: queries Stripe session status
  and updates users.subscription_tier to 'pro' on payment success
- Convert success page to Client Component: auto-calls verify API
  on load to immediately activate subscription
- Add missing columns to subscribers table (plan, plan_status,
  stripe_customer_id) for webhook compatibility
- Manually activate amorainsight@gmail.com Pro tier